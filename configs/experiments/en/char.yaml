defaults:
  - _self_
  - /base@_here_
  - /base/en@_here_

tokenizer:
  type: char_level
  vocab_size: 32768
  min_frequency: 20

experiment:
  id: "en/char"
  description: "Char-level tokenizer with 32k vocabulary size for English language"

generation:
  do_sample: true
  temperature: 0.5
  max_new_tokens: 512
